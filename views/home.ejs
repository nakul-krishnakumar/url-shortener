<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>URL Shortener</title>
</head>
<body style="background-color: rgb(13, 13, 51); color: aliceblue;">
   <div style="
      display: flex; 
      width: 100wh; 
      height: 100vh; 
      justify-content: center; 
      align-items: center;"
   >  
   <div class="wrapper" style="background-color: aliceblue; color: rgb(13, 13, 51); padding: 0rem 3rem 3rem 3rem; border-radius: 5px; margin: 0 auto;">
      <h1>Hi, <%= locals.name %></h1>
      <h2>Generate Shorthand URL for any URL !</h2>
      <form method="POST" action="/api/url">
         <label for="redirectURL">Enter URL here: </label>
         <input type="text" min="10" name="url" placeholder="https://example.com" size="50px"/>
         <button>Generate</button> 
      </form>

      <% if (locals.id) { %>
       <p style="color: green; font-weight: 500;">URL Generated : 
         <a href="http://localhost:8080/api/<%= id %>" target="_blank">http://localhost:8080/api/<%= id %></a>
       </p>
      <% } %>
   </div>
   </div>
</body>
</html>